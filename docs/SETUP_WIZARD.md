# Setup Wizard

The setup flow is exposed through `/api/setup`.

Use it to:

- check database health
- check storage health
- validate AWS
- validate SMTP
- create the first admin account

Once an admin exists, setup is locked automatically.
